# 珊瑚音乐 · 升级路线图（ROADMAP）

> 在线音乐播放器（纯静态 · 可部署 Cloudflare Pages）
> **状态：阶段 1–4 已完成；阶段 5「体验深化」5A–5G ✅ 全部交付；阶段 6「云端能力」6A/6B/6D ✅（6C 待开发）。**
> 6D 为鉴权升级：v1「口令即密钥」→ v2「账号密码 + Bearer 会话」，旧口令空间可一键接管。

---

## 当前能力基线

**已实现**：四类音源（本地上传 IndexedDB / 内置示例曲 / 远程链接 / 在线多音源 GD 音乐台 + Audius + Jamendo + iTunes）；Web Audio 真实频谱可视化（跨域自动降级）；拖拽排序；LRC 歌词（编码探测 / offset / 逐字卡拉OK / 双语合并 / 多来源匹配弹窗）；深色模式；键盘快捷键；进度续播；ID3 真实封面；多歌单与收藏；独立播放队列；搜索过滤；三段 EQ；倍速播放；睡眠定时；播放淡入淡出；桌面浮动歌词；歌词偏移校准；PWA 离线可安装；歌单 JSON 备份恢复；部署前语法校验。

**已知边界**：无系统级媒体控制（锁屏 / 线控）；无播放历史与统计；无跨设备同步；远程音频跨域时真实频谱仍降级；EQ 仅三段且只对本地歌链路生效；无自动化测试（仅语法校验）。

---

## 阶段划分（历史）

### 阶段 1 · 稳定与体验（纯静态可落地）— ✅ 已完成
| 条目 | 优先级 | 落地方式 | 状态 |
|------|--------|----------|------|
| 进度续播：记住每首歌关闭前的播放位置 | P0 | player.js 节流写入 + load 时 seek | ✅ |
| 移动端触控拖拽排序 | P0 | playlist.js Pointer Events + `.drag-handle` | ✅ |
| 真实专辑封面：解析音频 ID3v2 APIC 帧 | P1 | 零依赖 id3.js | ✅ |
| 部署前语法校验（CI） | P1 | scripts/check.js + Pages 构建命令 | ✅ |

### 阶段 2 · 功能补全（纯静态）— ✅ 已完成
| 条目 | 优先级 | 落地方式 | 状态 |
|------|--------|----------|------|
| 多歌单与收藏夹管理 | P1 | library.js：曲库单一事实源 + 歌单存 id 引用 | ✅ |
| 独立播放队列（与歌单解耦） | P1 | queue.js：追加 / 插队 / 拖拽重排 / 移除 | ✅ |
| 搜索与过滤（歌名 / 歌手） | P2 | 左栏搜索框实时过滤 | ✅ |
| 均衡器 EQ 与音效预设 | P2 | player.js 三段 BiquadFilter 链 | ✅ |

### 阶段 3 · 架构与部署 — ✅ 纯静态部分完成
| 条目 | 优先级 | 落地方式 | 状态 |
|------|--------|----------|------|
| PWA：离线缓存 + 可安装 | P1 | manifest.webmanifest + sw.js（network-first） | ✅ |
| 歌单导出 / 导入（JSON 分享） | P2 | 纯前端 | ✅ |
| R2 + Functions 持久化歌单（跨设备同步） | P2 | Cloudflare R2 + Workers | ⏳ 归入阶段 6 |
| Worker 代理远程音频（恢复真实频谱） | P2 | Cloudflare Worker 加 CORS 头 | ⏳ 归入阶段 6 |

### 阶段 4 · 在线音源扩展 — ✅ 已完成
| 条目 | 优先级 | 落地方式 | 状态 |
|------|--------|----------|------|
| 在线音乐多音源架构（音源注册表 + 二次解析管线） | P0 | online.js `registerSource` / `prepare` | ✅ |
| GD 音乐台（聚合多平台全曲库） | P0 | sources.js：search + url/pic/lyric 二次解析 | ✅ |
| Jamendo（CC 完整曲目） / iTunes（30 秒试听） | P1 | sources.js | ✅ |
| Audius OAuth 2.0 PKCE 账号登录 | P2 | auth.js：纯前端 S256 授权码 | ✅ |
| 播放健壮性：入库前校验 + 失效曲自动跳过 | P0 | verify/prepare + 失败限流跳过 | ✅ |
| 歌词多来源（GD 含翻译 / LRCLIB / lyrics.ovh 兜底） | P1 | lyrics.js 回退链 + 手动匹配弹窗 | ✅ |

---

## 阶段 5 · 体验深化（本轮范围 · 全量 · 纯静态）

> 原则：零依赖、纯静态、不引入后端。分 7 个批次提交，每批独立可验证、可回滚。
> 进度：5A–5G ✅ —— **阶段 5「体验深化」全部交付**

### 5A · 工程基座（P0）— ✅ 已完成
| 条目 | 技术依赖 | 落地点 | 状态 |
|------|----------|--------|------|
| 零依赖测试套件（LRC 解析 / 队列索引 / 音源归一化 / 数据层 / 媒体控制 / 淡入淡出） | node --test（无第三方框架） | `tests/*.test.js` + `tests/helpers.js` 沙箱加载器 | ✅ |
| 语法校验升级为「校验 + 测试」双闸门 | Node | scripts/check.js（114 用例） | ✅ |
| 真浏览器交互探针（无头 Chrome + CDP 真实鼠标事件） | CDP + Node 原生 WebSocket | tests/probe/ui-probe.js（16 项断言） | ✅ |
| 顺手修掉的两处真实缺陷 | — | ① `[offset:+500]` 正号偏移解析失效；② 淡入淡出算出负音量导致赋值抛异常、音量卡 0（听感=点了播放没声音） | ✅ |

### 5B · 系统集成（P0）— ✅ 已完成
| 条目 | 技术依赖 | 落地点 | 状态 |
|------|----------|--------|------|
| 系统媒体控制：锁屏 / 通知栏 / 耳机线控 | Media Session API | player.js：metadata + 8 个 action handler | ✅ |
| 系统媒体进度条与播放状态同步 | `setPositionState` / `playbackState` | player.js（每秒同步，切歌/跳转即时同步） | ✅ |
| 媒体键映射到播放控制（上下曲 / 播放暂停 / ±10 秒 / 定位 / 停止） | Media Session | player.js 具名 next/prev 复用 | ✅ |
| 不支持环境静默降级 | — | 无 `navigator.mediaSession` 时不影响播放 | ✅ |

### 5C · 数据洞察（P0）— ✅ 已完成
| 条目 | 技术依赖 | 落地点 | 状态 |
|------|----------|--------|------|
| 播放次数与最近播放时间记录 | localStorage（`cm-stats`） | player.js `markPlayed` + `played` 事件 | ✅ |
| 内置「最近」「最常播」虚拟歌单（按统计实时生成，不落歌单表） | 纯前端 | app.js `virtualTracks` + renderTabs | ✅ |
| 曲库统计面板（总数 / 总时长 / 来源分布 / 播放排行 Top 10 / 清空统计） | 纯前端 | 顶栏 📊 统计弹窗 | ✅ |
| 播放后回写真实时长（搜索接口多不返回时长） | `loadedmetadata` | app.js 监听 `meta` 事件 | ✅ |

### 5D · 音频引擎（P1）— ✅ 已完成
| 条目 | 技术依赖 | 落地点 | 状态 |
|------|----------|--------|------|
| 交叉淡入淡出（crossfade，可调 0–8 秒） | 双池四通道（可分析 A1/A2 + 直连 B1/B2） | player.js `maybeCrossfade` / `startCrossfade` | ✅ |
| 十段均衡器 + 音效预设（平坦 / 流行 / 摇滚 / 古典 / 人声 / 低音增强） | 10 条 BiquadFilter 串联 | player.js + 🎛 音效弹窗 | ✅ |
| 变速不变调开关（保留 / 不保留音高） | `preservesPitch`（含 moz / webkit 前缀） | player.js + 音效与倍速弹窗双入口同步 | ✅ |
| AB 段循环（选段重复，练歌 / 精听场景） | 纯前端 | player.js `enforceAbLoop` + 进度条下方 AB 控件 | ✅ |
| 音量均衡（不同歌曲响度差异自动补偿） | Web Audio `GainNode` + RMS 估计 | player.js `updateLoudness`（5Hz 采样、0.5–2 倍、秒级时间常数） | ✅ |

> **交叉语义**：仅在自动切歌（播到接近结尾）时启用，手动切歌保持即时切换 —— 用户已明确表达意图时延迟切换反而迟钝。
> 单曲循环 / AB 循环 / 播完停止 / 列表末曲均不触发交叉；AB 段在换歌时自动清除（选区只对某一首歌有意义）。
> **通道模型**：交叉要求两个元素同时发声，故由「本地 / 远程二元」升级为双池四通道。元素与池的归属固定
> （`MediaElementSource` 每个元素只能创建一次），切歌时同池轮换元素。元素创建顺序固定为 A1、B1、A2、B2。
> 旧版三段 EQ 数据由 `normalizeEQ` 映射到十段，升级不清空用户设置。

### 5E · 曲库管理（P1）— ✅ 已完成
| 条目 | 技术依赖 | 落地点 | 状态 |
|------|----------|--------|------|
| 歌单重命名（内联编辑，回车确认 / Esc 取消） | 纯前端 | app.js `startRenameTab` + 激活标签上的 ✎ | ✅ |
| 批量多选与批量操作（加入歌单 / 队列 / 删除） | 纯前端 | playlist.js 多选态 + 左栏批量工具条 | ✅ |
| 排序选项（默认 / 添加时间 / 歌名 / 歌手 / 时长 / 播放次数 / 最近播放） | 纯前端 | library.js `sortTracks` + 左栏下拉 | ✅ |
| 重复歌曲检测与合并（逐组确认保留项） | 纯前端（标题 + 歌手规范化指纹） | library.js `findDuplicates` + 🧹 去重弹窗 | ✅ |

> **排序**：`default` 保持歌单自身 id 顺序（零行为变更），其余六种由 `sortTracks` 处理；
> 虚拟歌单「最近 / 最常播」自带排序，不叠加用户排序以免语义打架。偏好持久化到 `cm-sort`。
> **去重指纹**：标题与歌手规范化（小写、去扩展名、**去括号及内容**、去标点空白）后拼接。
> 刻意不含时长 —— 同一首歌来自不同音源时常差几秒（试听版 / 完整版），带上反而漏判；
> 代价是「同名不同版本」可能归为一组，因此**必须逐组人工确认保留项**，不做静默自动删除。
> 组内默认保留「信息最完整」的一条（有时长 / 真实封面 / 内嵌歌词 / 本地或云端来源加分）。
> **顺带修掉的真实可用性缺陷**：`.list-tabs` 缺 `min-width: 0`，flex 项目被内容撑出面板边界后被
> 相邻面板覆盖 —— 歌单一多，右侧标签「看得见却点不到」；现改为容器内横向滚动并自动把活动标签滚入视野。

### 5F · 歌词与视觉（P1）— ✅ 已完成
| 条目 | 技术依赖 | 落地点 | 状态 |
|------|----------|--------|------|
| 全屏沉浸歌词模式（Esc 退出 / 点行跳转） | 覆盖层 + Esc 处理 | 新增全屏歌词层，与侧栏共享一份渲染结果 | ✅ |
| 歌词字号与行距调节（5 档，行距联动） | CSS 变量 `--lyric-size` / `--lyric-lh` | 歌词面板 A－ / A＋，偏好存 `cm-lyric-scale` | ✅ |
| 歌词导出为 `.lrc` 文件（含无时间轴提示） | Blob 下载 | app.js `exportLrc` / `downloadText` | ✅ |
| 可视化多模式（频谱条 / 波形 / 环形 / 极简） | Canvas | visualizer.js 模式注册表 + 封面区按钮 | ✅ |
| 可视化全屏（同一 canvas 放大铺满） | CSS 全屏类 + 尺寸重算 | app.js `toggleVizFull` | ✅ |

> **歌词视图统一**：侧栏与全屏歌词不再各写一套 DOM 逻辑，改为 `lyricViews` 持有两组
> 「行元素 / 逐字元素」，`syncLyrics` 一次同步两处 —— 否则两份逻辑很快会走偏且难以察觉。
> 滚动只对可见视图执行（隐藏容器 `clientHeight` 为 0）。
> **全屏实现选型**：用 `position: fixed` 覆盖层而不是 Fullscreen API —— 移动端 Safari 不支持
> 任意元素全屏，且覆盖层能自己接管 Esc（Fullscreen 的 Esc 由浏览器接管，两层全屏会互相打架）。

### 5G · 交互与移动端（P2）— ✅ 已完成
| 条目 | 技术依赖 | 落地点 | 状态 |
|------|----------|--------|------|
| 快捷键扩展（空格 / ←→ / ↑↓ / M / F / L / Shift+L / Q / S / R / T / `/` / `?` / Esc） | 纯前端 | app.js `HOTKEYS` + ⌨ 一览弹窗 | ✅ |
| 移动端迷你播放条 + 手势（左右滑切歌、上滑展开、点击展开） | Pointer Events | `#mini-bar` + ≤720px 布局层 | ✅ |
| Toast 类型化（info / ok / err）与空态文案统一 | 纯前端 | `toastOk` / `toastErr` + `emptyHintText` | ✅ |

> **手势实现要点（都是实测踩出来的，不是照抄文档）**：
> ① `touch-action: none` —— 用 `pan-y` 时浏览器会接管纵向手势，上滑展开永远收不到 `pointerup`；
> ② `pointerdown` 时 `setPointerCapture` —— 否则指针滑出迷你条后 move/up 不再回传，手势无法结算；
> ③ 指针捕获会改写 click 的 target，因此「点击」不靠 click 事件，而在 `pointerup` 里用「未移动 + 按下时的目标」判定；
> ④ ≤720px 下提示条要抬到迷你条之上 —— 它 z-index 更高，停在底部会把迷你条整片盖住（`elementFromPoint` 命中的是 toast）。
>
> 快捷键不抢浏览器组合键（带 Ctrl / Meta / Alt 一律放行）；按钮聚焦时空格交给按钮自己处理，
> 否则会「按下按钮一次、又切换播放一次」。空态文案改为「一句话现状 + 一句怎么开始」，
> 并区分「搜索无结果 / 空歌单 / 无播放记录」三种场景。

---

## 阶段 6 · 云端能力（Cloudflare Pages Functions + R2 + D1）— ✅ 已交付

> R2 存储桶 `music-audio`、D1 库 `online-music-db`（UUID `adf62ad1-0c09-4c7f-bcbc-3452e1f7fea0`）已创建，
> Pages 项目需绑定 `MUSIC_BUCKET`（R2）与 `DB`（D1）两个变量。
>
> **鉴权在 6D 从「口令即密钥」升级为「账号密码 + Bearer 会话」**：
> 注册时服务端随机分配 20 位十六进制空间标识（与密码无关），密码只存加盐 PBKDF2-SHA256 派生值；
> 写操作一律要求 `Authorization: Bearer <令牌>`，只有音频播放用 `?s=<slug>`（`<audio src>` 无法带自定义头）。
> 因此 slug 泄漏只等于「只读可播放」泄漏，不影响写权限，也无法从 slug 反推账号。
>
> v1 的口令空间没有丢失 —— 通过 `/api/auth/adopt` 可用旧口令把数据**复制**接管到账号名下（只复制、不删源，
> 因为那个口令验证不了归属，不具备破坏性副作用才安全）。

### 6A · Pages Functions 后端（P0）— ✅ 已完成
| 条目 | 落地点 | 状态 |
|------|--------|------|
| 纯逻辑层：key 规约、Range 解析、同步负载白名单（v1 口令 slug 仅留作接管） | `functions/_lib/core.mjs` | ✅ |
| 账号纯逻辑：PBKDF2 派生、用户名/密码规则、东八区时间、用户视图 | `functions/_lib/account.mjs` | ✅ |
| 会话层：Bearer 签发/校验/滑动续期/登出、登录限流、requireCloud 组合判断 | `functions/_lib/session.mjs` | ✅ |
| 账号表数据访问层（注册落库、登录善后、迁移标记） | `functions/_lib/users.mjs` | ✅ |
| 云端音频库：列表 / 上传 / 流式播放（Range）/ 删除 | `functions/api/audio/index.mjs`、`functions/api/audio/[id].mjs` | ✅ |
| 跨设备同步：歌单 / 远程曲目 / 歌词 / 设置 / 统计 / 播放进度 | `functions/api/state.mjs` | ✅ |
| 在线音源代理（服务端拉流绕开跨域 → 恢复真实频谱，EQ 对在线歌生效） | `functions/api/proxy.mjs`（域名白名单 + 空间归属校验） | ✅ |
| 链路自检端点（排查 R2 / D1 绑定、账号表、登录态、曲目数） | `functions/api/ping.mjs` | ✅ |
| D1 建表脚本（users / sessions / login_attempts + 三个索引） | `migrations/0000_accounts.sql` | ✅ |

### 6B · 前端云能力（P0）— ✅ 已完成
| 条目 | 落地点 | 状态 |
|------|--------|------|
| 账号模块：注册 / 登录 / 登出 / 会话持久化 / 旧口令接管 | `js/account.js` | ✅ |
| 云能力模块：登录态、请求封装、上传、列表、同步、代理地址组装 | `js/cloud.js` | ✅ |
| 云端面板：登录注册表单 / 账号条 / 上传本地歌曲 / 云端曲库（播放·入库·删除）/ 存到云端 / 从云端恢复 / 接管旧空间 | 顶栏「☁️ 云端」 | ✅ |
| 云端与代理音频接入 Web Audio（真频谱 + EQ 生效） | `player.js` `isAnalysable()` 同源判定 | ✅ |
| Service Worker 不拦截 `/api/*`（206 分片进不了 Cache API，音频体量大） | `sw.js` | ✅ |
| 无 Functions 环境下的可操作降级提示（本地静态调试不静默失败） | `app.js` `cloudHintFor()` | ✅ |
| 云端链路端到端验证（真实 Pages 运行时 + 本地模拟 R2/D1，67 项断言） | `tests/probe/cloud-e2e.mjs` | ✅ |

### 6D · 账号密码登录（替代 v1「口令即密钥」）— ✅ 已完成
| 条目 | 落地点 | 状态 |
|------|--------|------|
| 注册 / 登录 / 登出 / 当前账号 四个端点 | `functions/api/auth/{register,login,logout,me}.mjs` | ✅ |
| 旧口令空间接管（分批搬运、幂等、只复制不删源、记录 migrated_from） | `functions/api/auth/adopt.mjs` | ✅ |
| 密码安全：每账号随机盐 + 可配迭代数；迭代数提高后老账号登录时自动升级 | `_lib/account.mjs` `noteLogin()` | ✅ |
| 防账号枚举：账号不存在也走一次等价哈希，密码错与账号不存在返回同一文案 | `login.mjs` | ✅ |
| 防爆破：15 分钟窗口内 8 次失败后限流（跨 isolate 一致，落 D1） | `_lib/session.mjs` | ✅ |
| 会话安全：30 天有效期 + 剩余不足 7 天自动续期 + 登出只删令牌不动数据 | `_lib/session.mjs` | ✅ |
| 本地播放免登录，仅云端功能需登录（与 idle-exchange 策略一致） | `app.js` `renderCloudAuth()` | ✅ |
| 用真实运行时实测 PBKDF2 迭代数与 CPU 预算 | `scripts/probe-auth-cost.mjs`、cloud-e2e 打印登录耗时 | ✅ |

### 6C · 分享与配额（P1）— ⏳ 待开发
| 条目 | 技术依赖 | 状态 |
|------|----------|------|
| 分享歌单短链（key 规约里已预留 `share/<code>.json`） | 复用 R2 + 只读端点 | ⏳ |
| 云端音频去重与配额提示 | Functions | ⏳ |
| 服务端聚合音源（规避前端限流、隐藏子源细节） | Workers | ⏳ |
| 密码找回 / 邮箱验证（当前忘记密码只能重新注册） | 需 Resend 或 Cloudflare Access，参考 idle-exchange | ⏳ |

---

## 阶段 7 · 生态扩展（按需）

| 条目 | 优先级 | 技术依赖 |
|------|--------|----------|
| Tauri 桌面端（全局媒体键 / 系统托盘 / 独立浮窗歌词） | P3 | Tauri |
| 主题皮肤自定义（珊瑚橙之外的配色方案） | P3 | CSS 变量扩展 |
| 智能歌单 / 电台模式（按流派、时长、听歌习惯自动生成） | P3 | 本地规则引擎 |
| 播客 RSS 支持（订阅、长音频续播、跳过片头） | P3 | 纯前端 RSS 解析 |
| 歌词分享卡片（导出为图片） | P3 | Canvas 导出 |

---

## 落地原则

1. 始终**零依赖、无构建、纯静态**可部署 Cloudflare Pages；不引入 npm 运行时依赖。
2. 改动数据模型前先预留扩展位，避免后续批次返工。
3. 优先做纯静态可落地的条目；需 Functions / R2 的单独标记，等账号方案明确再动。
4. **交互类改动必须用无头浏览器探针做真实事件 + 断言验证**（本项目曾出现「dump 全绿但拖拽失效」「SW cache-first 卡旧版 JS」两次教训）。
5. 每批功能完成即 `commit + push`，Cloudflare Pages 自动重新部署并跑校验 + 测试双闸门。
