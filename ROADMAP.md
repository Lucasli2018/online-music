# 珊瑚音乐 · 升级路线图（ROADMAP）

> 在线音乐播放器（纯静态 · 可部署 Cloudflare Pages）
> 状态：阶段 2 已完成（多歌单/收藏、独立队列、搜索过滤、EQ）

## 当前能力基线
已实现：三音源（本地上传 IndexedDB / 远程链接 / 内置示例曲）、Web Audio 真实频谱可视化（远程自动降级）、拖拽排序、LRC 歌词同步、深色模式、键盘快捷键（空格 / ← →）、进度续播、ID3 真实封面、CI 语法校验、多歌单与收藏、独立播放队列、搜索过滤、三段均衡器 EQ。
已知边界：无 PWA/离线、无跨设备同步（R2 + Functions）、远程音频跨域时真实频谱仍降级、EQ 仅对本地歌链路生效。

## 阶段划分

### 阶段 1 · 稳定与体验（进行中 · 纯静态可落地）
| 条目 | 优先级 | 技术依赖 | 落地方式 | 状态 |
|------|--------|----------|----------|------|
| 进度续播：记住每首歌关闭前的播放位置，下次打开自动续播 | P0 | localStorage（按 trackId） | player.js 节流写入 + load 时 seek | ✅ 本次 |
| 移动端触控拖拽排序 | P0 | Pointer Events + 拖拽手柄 | playlist.js 重写拖拽，新增 `.drag-handle` | ✅ 本次 |
| 真实专辑封面：解析音频 ID3v2 APIC 帧 | P1 | 零依赖 id3.js（ArrayBuffer 解析） | 上传时提取 dataURL 随 record 存 IndexedDB | ✅ 本次 |
| 部署前语法校验（CI） | P1 | Node + Cloudflare Pages 构建命令 | scripts/check.js + 构建命令 `node scripts/check.js` | ✅ 本次 |

### 阶段 2 · 功能补全（纯静态为主）— ✅ 已完成
| 条目 | 优先级 | 技术依赖 | 落地方式 | 状态 |
|------|--------|----------|----------|------|
| 多歌单与收藏夹管理 | P1 | 曲库单一事实源 + 歌单存 id 引用（localStorage） | library.js：全部/收藏/自定义歌单，歌曲只在曲库存一份 | ✅ 本次 |
| 独立播放队列（与歌单解耦） | P1 | 纯前端 | queue.js：追加/插队/拖拽重排/移除，驱动播放器 | ✅ 本次 |
| 搜索与过滤（按歌名 / 歌手） | P2 | 纯前端 | 左栏搜索框实时过滤当前歌单 | ✅ 本次 |
| 均衡器 EQ 与音效预设 | P2 | Web Audio BiquadFilter 链 | player.js 三段滤波（lowshelf/peaking/highshelf），仅作用于本地歌 | ✅ 本次 |

### 阶段 3 · 架构与部署（需 Cloudflare 进阶能力）
| 条目 | 优先级 | 技术依赖 | 落地方式 |
|------|--------|----------|----------|
| PWA：离线缓存 + 可安装 | P1 | manifest.webmanifest + sw.js | 纯静态可托管，契合 Pages |
| R2 + Functions 持久化歌单（跨设备同步） | P2 | Cloudflare R2 + Workers | 需后端与账号配置，工作量较大 |
| Worker 代理远程音频（恢复真实频谱） | P2 | Cloudflare Worker（加 CORS 头） | 解决远程歌被跨域静音/无频谱问题 |
| 歌单导出 / 导入（JSON 分享） | P2 | 纯前端 | 低成本，建议紧随 PWA |

### 阶段 4 · 生态扩展（按需）
| 条目 | 优先级 | 技术依赖 |
|------|--------|----------|
| 分享歌单链接（可播放清单） | P3 | URL 编码 或 R2 短链 |
| 社区示例曲库 / 公共电台 | P3 | 需后端 + 版权评估 |
| 桌面端打包（Tauri） | P3 | Tauri |
| 主题皮肤自定义 | P3 | CSS 变量扩展 |

## 落地原则
1. 始终零依赖、单文件可双击运行、纯静态可部署 Cloudflare Pages。
2. 改动数据模型（如多歌单）前先预留扩展位，避免阶段 2 返工。
3. 优先做纯静态可落地的条目；需 Functions/R2 的标记清楚，等需求明确再动。
4. 每次功能提交即 `commit + push`，Cloudflare Pages 自动重新部署并跑语法校验。
